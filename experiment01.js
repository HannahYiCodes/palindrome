// randomizer dog toy box subscription
// levels of subscription = more variety of toys and exclusive categories

// TEST
// radio buttons of dog owner requests that dictate the categories of dog toys

// weight, age

// 5-10, 10-25, 25-40, 40-60, 60-80, 80-100, 100-125+
// puppy, adult, senior

let allDogToys = [{
    weightInput_0: `miniRope miniKong miniChew`,
    weightInput_1:``,

    // mini, small, small+, medium, medium+, large, large+
}]

const userDogWeight = document.getElementById('dogWeightInput').value
let maybeToys = []
let finalBox = []

dogWeight = () => {
    let weightInput = ['5-10', '10-25', '25=40', '40-60', '60-80', '80-100', '100-125+']
    for (let i = 0; i < weightInput.length; i++) {
        if (userDogWeight = weightInput[0]) {
            maybeToys.push()
            // randomized category of toys + some toys can be the same
            // chooses a specified section of the array and THEN PUSHED into the finalized box array
            allDogToys.weightInput_0[0]
        }
        if (userDogWeight = weightInput[1]) {
            
        }
        if (userDogWeight = weightInput[2]) {
            
        }
        if (userDogWeight = weightInput[3]) {
            
        }
        if (userDogWeight = weightInput[4]) {
            
        }
        if (userDogWeight = weightInput[5]) {
            
        }
        if (userDogWeight = weightInput[6]) {
            
        }
    }
}